// 이전 쉐이더 결과를 받아서 입력값을 처리
// 1픽셀 마다 호출됨

// 마찬가지로 PS도 마음대로 이름 설정할수있음

// 픽셀 쉐이더 : 픽셀 셰이더는 그리고자 하는 도형에 색상을 입힐 때를 위한 작은 프로그램입니다.
//				 렌더링 될 가각의 픽셀들의 색을 계산하는 방식의 쉐이더

float4 PS(float4 p: POSITON) : SV_TARGET
{	
	return float4(1,0,0,1); // 색상 변경 가능
}